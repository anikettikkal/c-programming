#include <stdio.h>
// area of square
int main() {
    float side;
    printf("enter the value of side:");
    scanf("%f",&side);
    printf("area of square is :%f", side*side);
    return 0;
}
